// This package has no Dart-facing API. The app talks to the native
// platform view directly via UiKitView + MethodChannel/EventChannel — see
// lib/services/native_vlc_kit_backend.dart in the main app. This file only
// exists because a Flutter plugin package requires a lib/ entrypoint.
